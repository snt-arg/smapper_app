/**
 * Represents the metadata of the onboard computer.
 *
 * @interface OnboardPCMetadata
 * @property {string} model - The model name or identifier of the onboard PC..
 */
export interface OnboardPCMetadata {
  model: string
}
